# Comma-separated allowed CORS origins — no trailing slashes
# Production example: https://your-app.vercel.app
ALLOWED_ORIGINS=http://localhost:3000

# USC class term code — update each semester (check classes.usc.edu)
TERM_CODE=20263
